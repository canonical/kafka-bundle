applications:
  kafka:
    channel: latest/edge
    charm: kafka
    constraints: arch=amd64
    num_units: 1
    revision: 75
  zookeeper:
    channel: latest/edge
    charm: zookeeper
    constraints: arch=amd64
    num_units: 3
    revision: 82
relations:
- - zookeeper:zookeeper
  - kafka:zookeeper
