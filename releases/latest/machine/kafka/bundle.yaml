name: kafka-latest
series: focal
applications:
  kafka:
    channel: edge
    charm: kafka
    constraints: arch=amd64
    num_units: 1
  zookeeper:
    channel: edge
    charm: zookeeper
    constraints: arch=amd64
    num_units: 3
relations:
- - zookeeper:zookeeper
  - kafka:zookeeper
