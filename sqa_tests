#!/usr/bin/env bash
# Runs the Solutions QA integration tests (tox -e integration-solqa),
# forwarding any arguments to the underlying pytest command.
set -euo pipefail

exec tox -e integration-solqa -- "$@"
