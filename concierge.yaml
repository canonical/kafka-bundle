juju:
  model-defaults:
    logging-config: <root>=INFO; unit=INFO
providers:
  lxd:
    enable: true
    bootstrap: true
host:
  snaps:
    jhack:
      channel: latest/edge
      connections:
        - jhack:dot-local-share-juju snapd